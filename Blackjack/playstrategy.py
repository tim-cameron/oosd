class StrategyA:

        cards_played = []
	def __init__(self):
		.


	def hits(self, cards_played):
            this.cards_played = cards_played
	    if (raw_input("hit or stand? (h/s) :") == "h"):
                return True
            else:
                return False

        def calculate_total(self):
            total = 0;
            for i = 0 in xrange(0, len(self.cards_played)):
                total = total + self.cards_played[i][1] ## [card][0 = card name, 1 = card value]

            if ( len(self.cards_played) >= 16 )
